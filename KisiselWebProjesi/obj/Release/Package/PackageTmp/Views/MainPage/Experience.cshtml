@using KisiselWebProjesi.Models.Classes
@model List<Experience>

@*<br />
<h2>Deneyimler</h2>
<hr style="width:200px">
<br />
<form style="text-align: left;">
    @foreach (var x in Model)
    {
        <h4>@x.experience_name</h4>
        <p>@x.experience_description</p>
        <br />
    }
</form>*@

@*<div class="card" style=" border-radius: 20px; height: auto; min-height: 200px; background: linear-gradient(111deg, #20b2aa 0%, #4682b4 100%); ">
    <div class="section-title" style="font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;font-size:30px">Deneyimler</div>
    @foreach (var x in Model)
    {
        <div class="experience-item">
            <div class="experience-title" style="font-style:italic;font-weight:bold">@x.experience_name.</div>
            <div class="experience-description">@x.experience_description</div>
        </div>
    }
</div>*@

<div class="row">
    @foreach (var x in Model)
    {
        <div class="col-sm-4">
            <div class="experience-card">
                <div class="experience-title">@x.experience_name</div>
                <div class="experience-description">
                    @x.experience_description
                </div>
            </div>
        </div>
    }

</div>
