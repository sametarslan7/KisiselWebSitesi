
@using KisiselWebProjesi.Models.Classes
@model List<Icons>


@*<div class="social">
        @foreach (var x in Model)
        {
            <a href="@x.icon_link"><i class="@x.icon_name"></i></a>
        }
    </div>*@

@*
<div class="social-media-section" style="margin-top:200px;">
    <div class="social-media-title">Sosyal Medya</div>
    @foreach (var x in Model)
    {
        <div class="social-media-icons">
            <div class="social-media-icon">
                <a href="@x.icon_link"><i class="@x.icon_name"></i></a>
            </div>
        </div>
    }

</div>*@

@foreach (var x in Model)
{

    <div class="socials mt-4">
        <a class="social-item" href="@x.icon_link"><i class="@x.icon_name"></i></a>
    </div>
}


