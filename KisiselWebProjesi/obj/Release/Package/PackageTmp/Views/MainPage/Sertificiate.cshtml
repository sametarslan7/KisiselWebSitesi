@using KisiselWebProjesi.Models.Classes
@model List<Sertificiate>

@*<br />
    <h2>Sertifikalar</h2>
    <hr style="width:200px">
    <br />
    <form style="text-align: left;">
        @foreach (var x in Model)
        {
            <h4>@x.sertificiate_name</h4>
            <p>@x.sertificiate_description</p>
            <br />
        }
    </form>*@

@*
@foreach (var x in Model)
{
        <!-- Sertifikalar burada resimli olarak gösterilecek -->
        <div class="project-item" style="padding-left:20px;">
            <div class="project-img-container">
                <img class="project-img" src="@x.sertificiate_description" alt="Sertifika 1">
            </div>
            <div class="sertificiate-title-item">@x.sertificiate_name</div>
        </div>
        <!-- Daha fazla sertifika ekleyebilirsiniz -->
}*@

<div class="row">
    @foreach (var x in Model)
    {
        <div class="col-sm-3">
            <div class="certificate-card">
                <img src="@x.sertificiate_description" alt="certificate">
                <div class="certificate-title">@x.sertificiate_name</div>
            </div>
        </div>
    }

</div>
